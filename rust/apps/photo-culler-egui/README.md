# egui + wgpu track

This compile-safe bootstrap establishes state ownership and the shared core boundary. The next increment adds `eframe`/`egui` and a wgpu texture cache after the catalog query DTO is versioned. No production readiness or GPU performance is claimed yet.
