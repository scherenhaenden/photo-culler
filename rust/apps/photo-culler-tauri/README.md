# Tauri + WebGL track

This compile-safe bootstrap contains the shared Rust boundary and a WebGL2 viewport prototype. The next increment adds the Tauri runtime and commands that consume a versioned application API. It intentionally does not duplicate catalog or scoring logic in JavaScript.
